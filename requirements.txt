# TradAlert runtime dependencies.
#
# Versions intentionally unpinned; add explicit pins (`==X.Y.Z`) once the
# project is on a CI / release cycle.

# Core data / numerics
pandas
numpy
pyarrow

# Exchange trading-day calendars (live data-freshness guards: partial-bar drop, staleness)
pandas_market_calendars

# Market data
yfinance>=1.4              # >=1.4 for yf.Calendars (econ/IPO calendar API); the earnings_dates surprise columns

# Config + secrets
pyyaml
python-dotenv

# Persistence
mysql-connector-python

# HTTP — macro/SEC fetchers + Wikipedia scrapers
requests
beautifulsoup4
lxml

# Charts
mplfinance
matplotlib
pillow

# Spreadsheet read for AAII sentiment
xlrd

curl-cffi
pytest

# Telegram push alerts + interactive bot (config/settings.yaml telegram.enabled)
python-telegram-bot
