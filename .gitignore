/config/secrets.env
/.venv/
/.idea/
/.claude/
/data/*
!/data/*.sql
# Manual test/study/verification RUN RESULTS live under docs/backtest_out/ (global
# rule: data/ is for automated program outputs only). Keep their logs/dumps out of
# git, same as data/.
/docs/backtest_out/
# PyCharm "Inspect Code" exports — local triage input, never committed
/errors_report/
__pycache__/
/logs/
*.tmp
*.corrupt
.opencodeignore
# Only README.md is tracked on GitHub; all other Markdown stays local
# (TODO, AGENTS, docs/ ADRs + plans + verification notes).
*.md
!README.md